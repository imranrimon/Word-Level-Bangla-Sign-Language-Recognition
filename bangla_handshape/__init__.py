"""Shared library for Bangla handshape image work (Path 1 + Path 3).

Imports are lazy — `from bangla_handshape import HandshapeDataset` works
without pulling in torch unless you actually use the dataset class.
"""
